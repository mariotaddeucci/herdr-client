# `aclose`

Close the subscription connection if it is open. Await this method when cleaning up a
subscription outside an `async with` block.

::: herdr_client.AsyncSubscription.aclose
    options:
      show_root_heading: false
      show_source: true
      show_signature_annotations: true
      separate_signature: true
