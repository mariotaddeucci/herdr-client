# `events`

Yield pushed event envelopes asynchronously until the server closes the socket. The
subscription must already be open, normally through an `async with` block.

::: herdr_client.AsyncSubscription.events
    options:
      show_root_heading: false
      show_source: true
      show_signature_annotations: true
      separate_signature: true
