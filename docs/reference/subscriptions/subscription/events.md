# `events`

Yield pushed event envelopes until the server closes the socket. The subscription must
already be open, normally through a `with` block.

::: herdr_client.Subscription.events
    options:
      show_root_heading: false
      show_source: true
      show_signature_annotations: true
      separate_signature: true
